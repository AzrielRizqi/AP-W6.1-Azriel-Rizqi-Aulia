Identify which class method is called What is the output of the program? Why are the two lines different?

Output dari program adalah "Vehicle is moving" dan "Car is Moving" dan method yang dipanggil adalah method move dari class Vehicle dan method move yang sudah di override dari class Car.

Program ini memiliki dua output berbeda karena method yang sama, move, telah di override pada child class (class Car) dan diubah isi dari method tersebut. Jadi ketika kedua method dipanggil, output yang ditampilkan berbeda.